server:
  port: 8080
  address: 0.0.0.0
  servlet:
    context-path: /bej3

spring:
  datasource:
    url: "jdbc:mysql://mysql:3306/new_bej_sp3?createDatabaseIfNotExist=true"
    username: root
    password: root
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  
  sql:
    init:
      mode: never

  web:
    cors:
      allowed-origins: "http://localhost:3000,http://localhost:5173"
      allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
      allowed-headers: "*"
      allow-credentials: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    stomp:
      port: 61613
    username: guest
    password: guest

  # DevTools - Hot Reload
  devtools:
    restart:
      enabled: true
      poll-interval: 2000ms
      quiet-period: 1000ms
    livereload:
      enabled: true

firebase:
  config:
    service-account-key-path: classpath:datn-d21-firebase-adminsdk-fbsvc-d2772bc9ae.json
    project-id: datn-d21
    database-url: https://datn-d21-default-rtdb.asia-southeast1.firebasedatabase.app/
    storage-bucket: datn-d21.firebasestorage.app
      
jwt:
  signerKey: "m0zp3DeeWY8ZsU0aELBjSYn4KEVFbimeaZRpgkF0DloyzilopHxqZyDW9dQjBYfD"
  

vnpay:
  payUrl: ${VNPAY_PAY_URL}
  returnUrl: ${VNPAY_RETURN_URL}
  ipnUrl: ${VNPAY_IPN_URL}
  tmnCode: ${VNPAY_TMN_CODE}
  hashSecret: ${VNPAY_HASH_SECRET}

zalopay:
  appId: ${ZALOPAY_APP_ID}
  key1: ${ZALOPAY_KEY1}
  key2: ${ZALOPAY_KEY2}
  createOrderUrl: ${ZALOPAY_CREATE_ORDER_URL}
  callbackUrl: ${ZALOPAY_CALLBACK_URL}
  returnUrl: ${ZALOPAY_RETURN_URL}

logging:
  level:
    root: INFO
    com.DATN.Bej: DEBUG